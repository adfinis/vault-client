pkgbot:
  download-delay: 11
  repo: adsy-testing
  branches:
    - packaging
  stages: 
    - package
  packages:
    debian:
      jessie:
        - vault-client*deb
      wheezy: 
        - vault-client*deb
      stretch:
        - vault-client*deb

