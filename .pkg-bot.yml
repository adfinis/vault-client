pkgbot:
  download-delay: 11
  repo: adsy-testing
  branches:
    - packaging
  stages: 
    - build
  packages:
    debian:
      jessie:
        - vault-client*deb
      wheezy: 
        - vault-client*deb
      stretch:
        - vault-client*deb
